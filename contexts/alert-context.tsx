// import { Alert } from "@/components/Shared/AlertHandler";
// import { createContext } from "react";

// const AlertContext = createContext(null);

// type AlertContext = {

// };

// const AlertContextProvider = () => {
//   const addAlert = (alert) => setAlerts([...alerts, alert])
//   return <AlertContext.Provider value={{}}></AlertContext.Provider>;
// };
